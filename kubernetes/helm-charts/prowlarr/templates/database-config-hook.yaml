{{- if .Values.prowlarr.postgres.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: "{{ .Release.Name }}-postgres-init"
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  template:
    metadata:
      name: "{{ .Release.Name }}-postgres-init"
    spec:
      restartPolicy: Never
      containers:
      - name: postgres-init
        image: "aidanhilt/atils-postgres-client"
        command: ["/bin/sh"]
        args:
        - -c
        - sleep 5000
        env:
        {{ if .Values.prowlarr.postgres.enabled }}
        - name: PGHOST
          value: {{ .Values.prowlarr.postgres.host }}
        - name: PGPORT
          value: {{ .Values.prowlarr.postgres.port | quote }}
        - name: PGUSER
          valueFrom:
            secretKeyRef:
              name: {{ .Values.prowlarr.postgres.setupJob.setupJobSecret }}
              key: {{ .Values.prowlarr.postgres.setupJobSecretUsernameKey }}
        - name: PGPASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Values.prowlarr.postgres.setupJob.setupJobSecret }}
              key: {{ .Values.prowlarr.postgres.setupJobSecretPasswordKey }}
        {{ end }}
        volumeMounts:
        - name: postgres-init-script
          mountPath: /mnt/setup.sql
          subPath: setup.sql
      volumes:
      - name: postgres-init-script
        configMap:
          name: prowlarr-postgres-init
{{- end }}