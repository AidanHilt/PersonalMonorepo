apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres-cluster
  namespace: postgres
spec:
  description: "Central Postgres cluster for qa-cluster"
  instances: 1
  startDelay: 300
  stopDelay: 300
  primaryUpdateStrategy: unsupervised

  superuserSecret:
    name: postgres-config

  enableSuperuserAccess: true

  storage:
    size: 1Gi
