apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: videos-auth
  namespace: videos
spec:
  method: kubernetes
  mount: videos/prowlarr
  kubernetes:
    role: prowlarr
    serviceAccount: prowlarr
  audiences:
    - vault
