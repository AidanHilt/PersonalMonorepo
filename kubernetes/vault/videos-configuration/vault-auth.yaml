apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: videos-auth
  namespace: videos
spec:
  method: kubernetes
  mount: videos/prowlarr
  kubernetes:
    role: prowlarr
    serviceAccount: prowlarr
# ===============================
# For tomorrow
# I think the pod needs the service account listed here, so we should just suck it up and make our principle of least privelige auths now
# ===============================
