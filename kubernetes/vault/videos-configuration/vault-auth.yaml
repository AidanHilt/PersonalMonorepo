apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: videos-auth
  namespace: videos
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: prowlarr
    serviceAccount: prowlarr
