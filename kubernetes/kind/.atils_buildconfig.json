{
  "actions": [
    {
      "name": "setup-cluster",
      "command": "kind create cluster --config ~/PersonalMonorepo/kubernetes/kind/ingress-cluster.yaml",
      "order": 0,
      "description": "Create the cluster"
    },
    {
      "name": "teardown-cluster",
      "command": "kind delete cluster",
      "order": 1,
      "description": "Delete the cluster"
    }
  ]
}