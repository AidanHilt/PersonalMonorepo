rxresu:
  appUrl: "http://qa-cluster-lb.lan"

  configSecret:
    existingSecret: rxresu-config-secret

serviceAccount:
  create: true
  name: rxresu
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "-2"
