gfd:
  enabled: true

runtimeClassName: nvidia

config:
  map:
    default: |-
      version: v1
      flags:
        migStrategy: none
        plugin:
          deviceListStrategy: "envvar"
          deviceIDStrategy: "uuid"
          passDeviceSpecs: false
