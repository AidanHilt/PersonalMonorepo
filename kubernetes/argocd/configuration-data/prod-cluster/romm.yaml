romm:
  configSecret: romm-config-secret

  database:
    enabled: true

redis:
  auth:
    existingSecret: romm-config-secret
    existingSecretPasswordKey: redisPassword

serviceAccount:
  create: true
  name: romm
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "-2"
