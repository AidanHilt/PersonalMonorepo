prowlarr:
  urlBase: prowlarr

  configSecret:
    existingSecret: prowlarr-config-secret

serviceAccount:
  create: true
  name: prowlarr
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "-2"
