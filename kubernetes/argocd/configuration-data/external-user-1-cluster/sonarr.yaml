sonarr:
  urlBase: sonarr

  configSecret:
    existingSecret: sonarr-config-secret

serviceAccount:
  create: true
  name: sonarr
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "-2"

persistence:
  config:
    enabled: true
