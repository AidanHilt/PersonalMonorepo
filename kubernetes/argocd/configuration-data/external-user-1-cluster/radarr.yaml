radarr:
  urlBase: radarr

  configSecret:
    existingSecret: radarr-config-secret

image:
  tag: latest

serviceAccount:
  create: true
  name: radarr
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "-2"

persistence:
  config:
    enabled: true
