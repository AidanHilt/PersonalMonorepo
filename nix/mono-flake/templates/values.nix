{ pkgs, ... }:

{
  username = "";

  hostname = "";

  # imports = [
  #  ../shared-values/<machine-group>.nix
  # ];

  # networking = {
  #   fixedIp = false;

  #   defaultGateway = "";
  #   nameservers = [];
  #   address = "";
  #   prefixLength = 24;
  # }

  # k8s = {
  #   primaryNode = false;

  #   clusterEndpoint = "";
  #   # Used to identify which secrets to provide to the cluster
  #   clusterName = "";
  # };
}